<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>QA Review</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"></script>
<style>
:root{
  --bg:#0f0f11;--bg2:#17171b;--bg3:#1e1e24;
  --border:rgba(255,255,255,0.08);--border2:rgba(255,255,255,0.14);
  --text:#f0eff4;--text2:#9898a8;--text3:#5c5c70;
  --accent:#7c6af7;--accent2:#a89cf8;
  --green:#3ecf8e;--green-bg:rgba(62,207,142,0.1);
  --red:#f87171;--red-bg:rgba(248,113,113,0.1);
  --amber:#fbbf24;--amber-bg:rgba(251,191,36,0.1);
  --orange:#fb923c;
  --blue:#60a5fa;
  --teal:#2dd4bf;
  --r:10px;--rl:16px;
}
*{box-sizing:border-box;margin:0;padding:0}
body{font-family:'DM Sans',sans-serif;background:var(--bg);color:var(--text);min-height:100vh;font-size:14px;line-height:1.6}
.app{display:flex;min-height:100vh}
.sidebar{width:230px;flex-shrink:0;background:var(--bg2);border-right:1px solid var(--border);padding:2rem 1rem;display:flex;flex-direction:column;gap:4px;position:sticky;top:0;height:100vh;overflow-y:auto}
.logo{font-size:11px;font-weight:600;letter-spacing:.1em;color:var(--text3);text-transform:uppercase;padding:0 .5rem 1.5rem;border-bottom:1px solid var(--border);margin-bottom:.5rem}
.logo span{color:var(--accent2)}
.ni{display:flex;align-items:center;gap:8px;padding:8px 10px;border-radius:8px;cursor:pointer;font-size:13px;color:var(--text2);transition:all .15s;border:none;background:none;width:100%;text-align:left;font-family:'DM Sans',sans-serif}
.ni:hover{background:var(--bg3);color:var(--text)}
.ni.active{background:rgba(124,106,247,.15);color:var(--accent2);font-weight:500}
.ni .sn{width:20px;height:20px;border-radius:50%;background:var(--bg3);display:flex;align-items:center;justify-content:center;font-size:10px;font-family:'DM Mono',monospace;flex-shrink:0;border:1px solid var(--border)}
.ni.active .sn{background:var(--accent);border-color:var(--accent);color:#fff}
.sep{height:1px;background:var(--border);margin:.75rem 0}
.main{flex:1;padding:2.5rem 2rem;max-width:960px}
.page{display:none}
.page.active{display:block;animation:fi .2s ease}
@keyframes fi{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
.ph{margin-bottom:2rem}
.ph h2{font-size:22px;font-weight:600;margin-bottom:4px}
.ph p{font-size:13px;color:var(--text2)}
.card{background:var(--bg2);border:1px solid var(--border);border-radius:var(--rl);padding:1.5rem;margin-bottom:1rem}
.ct{font-size:11px;font-weight:500;color:var(--text3);text-transform:uppercase;letter-spacing:.08em;margin-bottom:1.25rem;display:flex;align-items:center;gap:6px}
.ct::before{content:'';width:3px;height:12px;border-radius:2px;background:var(--accent)}
.f{margin-bottom:1rem}
label{display:block;font-size:12px;color:var(--text2);margin-bottom:5px;font-weight:500}
input[type=text],input[type=date],input[type=number],select,textarea{width:100%;background:var(--bg3);border:1px solid var(--border);border-radius:var(--r);padding:9px 12px;color:var(--text);font-family:'DM Sans',sans-serif;font-size:14px;outline:none;transition:border .15s}
input:focus,select:focus,textarea:focus{border-color:var(--accent)}
input[type=number]{-moz-appearance:textfield}
input[type=number]::-webkit-outer-spin-button,input[type=number]::-webkit-inner-spin-button{-webkit-appearance:none}
textarea{resize:vertical;min-height:100px}
select{appearance:none;cursor:pointer}
.g2{display:grid;grid-template-columns:1fr 1fr;gap:12px}
.g3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:12px}
.g4{display:grid;grid-template-columns:repeat(4,1fr);gap:12px}
/* dash cards */
.dc{background:var(--bg2);border:1px solid var(--border);border-radius:var(--rl);padding:1.25rem;position:relative;overflow:hidden}
.dc::before{content:'';position:absolute;top:0;left:0;right:0;height:3px;border-radius:var(--rl) var(--rl) 0 0}
.dc.cp::before{background:var(--accent)}
.dc.cg::before{background:var(--green)}
.dc.cr::before{background:var(--red)}
.dc.co::before{background:var(--orange)}
.dc.cb::before{background:var(--blue)}
.dc.ct2::before{background:var(--teal)}
.dl{font-size:11px;color:var(--text3);text-transform:uppercase;letter-spacing:.06em;margin-bottom:8px}
.dv{font-size:30px;font-weight:600;font-family:'DM Mono',monospace;line-height:1}
.dv.vp{color:var(--accent2)}.dv.vg{color:var(--green)}.dv.vr{color:var(--red)}
.dv.vo{color:var(--orange)}.dv.vb{color:var(--blue)}.dv.vt{color:var(--teal)}
.ds{font-size:11px;color:var(--text3);margin-top:6px}
/* table */
.tbl{width:100%;border-collapse:collapse}
.tbl th{font-size:11px;color:var(--text3);text-transform:uppercase;letter-spacing:.06em;padding:7px 8px;border-bottom:1px solid var(--border);text-align:left;font-weight:500;white-space:nowrap}
.tbl td{padding:4px 5px;border-bottom:1px solid var(--border);vertical-align:middle}
.tbl tr:last-child td{border-bottom:none}
.tbl input,.tbl select{padding:5px 8px;font-size:12px;background:var(--bg3);border:1px solid var(--border);border-radius:8px;color:var(--text);font-family:'DM Sans',sans-serif;outline:none;width:100%}
.tbl input:focus,.tbl select:focus{border-color:var(--accent)}
.tbl select{appearance:none}
/* misc */
.tb{display:inline-flex;align-items:center;gap:8px;background:rgba(124,106,247,.1);border:1px solid rgba(124,106,247,.25);border-radius:var(--r);padding:8px 14px;font-family:'DM Mono',monospace;font-size:14px;color:var(--accent2)}
.tbn{font-size:20px;font-weight:600}
.blocked-item{background:var(--bg3);border:1px solid var(--border);border-radius:var(--r);padding:1rem;margin-bottom:8px}
.rb{padding:4px 10px;border-radius:20px;font-size:11px;font-weight:500;border:1px solid var(--border);cursor:pointer;background:var(--bg3);color:var(--text2);transition:all .15s;font-family:'DM Sans',sans-serif}
.rb.ra{background:var(--red-bg);border-color:var(--red);color:var(--red)}
.rb.rm{background:var(--amber-bg);border-color:var(--amber);color:var(--amber)}
.rb.rl{background:var(--green-bg);border-color:var(--green);color:var(--green)}
.ti{display:flex;gap:8px;align-items:flex-start;margin-bottom:8px}
.btn{display:inline-flex;align-items:center;gap:6px;padding:9px 16px;border-radius:var(--r);font-size:13px;font-weight:500;cursor:pointer;border:1px solid var(--border2);background:var(--bg3);color:var(--text);transition:all .15s;font-family:'DM Sans',sans-serif}
.btn:hover{background:var(--bg2)}
.btnp{background:var(--accent);border-color:var(--accent);color:#fff}
.btnp:hover{background:#6a5ae0}
.btns{padding:6px 12px;font-size:12px}
.btnd{background:none;border-color:transparent;color:var(--text3);padding:5px 7px}
.btnd:hover{color:var(--red);background:var(--red-bg)}
.ab{display:flex;justify-content:flex-end;gap:8px;margin-top:2rem;padding-top:1.5rem;border-top:1px solid var(--border)}
.spbar{background:var(--bg2);border-bottom:1px solid var(--border);padding:.75rem 2rem;display:flex;align-items:center;gap:1.5rem;font-size:12px;color:var(--text3);position:sticky;top:0;z-index:10}
.spbar strong{color:var(--text);font-weight:500}
.sppill{background:rgba(124,106,247,.15);border:1px solid rgba(124,106,247,.3);color:var(--accent2);padding:2px 10px;border-radius:20px;font-size:12px;font-weight:500;font-family:'DM Mono',monospace}
.donut-wrap{position:relative;display:flex;align-items:center;justify-content:center;height:180px}
.donut-center{position:absolute;text-align:center;pointer-events:none}
.donut-center .dv{font-size:26px}
.donut-center .ds{margin-top:2px}
.chartbox{position:relative;height:220px}
.barrow{display:flex;align-items:center;gap:10px;margin-bottom:10px}
.barbg{flex:1;height:8px;background:var(--bg3);border-radius:4px;overflow:hidden}
.barfill{height:100%;border-radius:4px;transition:width .4s}
.expbox{background:var(--bg3);border:1px solid var(--border);border-radius:var(--r);padding:1.25rem;font-family:'DM Mono',monospace;font-size:12px;line-height:1.8;color:var(--text2);white-space:pre-wrap;max-height:420px;overflow-y:auto}
.info-row{background:var(--bg3);border:1px solid var(--border);border-radius:var(--r);padding:10px 14px;font-size:13px;color:var(--text2);display:flex;gap:20px;flex-wrap:wrap}
.counts-row{background:var(--bg3);border:1px solid var(--border);border-radius:var(--r);padding:7px 14px;font-size:12px;display:flex;gap:12px;align-items:center;flex-wrap:wrap}
.fixed-row{background:var(--green-bg);border:1px solid rgba(62,207,142,.2);border-radius:var(--r);padding:7px 14px;font-size:12px;color:var(--text2)}
::-webkit-scrollbar{width:6px}
::-webkit-scrollbar-track{background:var(--bg)}
::-webkit-scrollbar-thumb{background:var(--bg3);border-radius:3px}
</style>
</head>
<body>

<div class="spbar">
  <span class="sppill" id="bar-sprint">Sprint —</span>
  <span>QA: <strong id="bar-qa">—</strong></span>
  <span>Squad: <strong id="bar-squad">—</strong></span>
  <span>Período: <strong id="bar-period">—</strong></span>
</div>

<div class="app">
  <nav class="sidebar">
    <div class="logo">QA <span>Review</span></div>
    <button class="ni active" id="nav0" onclick="goTo(0)"><span class="sn">◈</span> Dashboard</button>
    <div class="sep"></div>
    <button class="ni" id="nav1" onclick="goTo(1)"><span class="sn">1</span> Informações gerais</button>
    <button class="ni" id="nav2" onclick="goTo(2)"><span class="sn">2</span> Resumo executivo</button>
    <div class="sep"></div>
    <button class="ni" id="nav3" onclick="goTo(3)"><span class="sn">3</span> Métricas de testes</button>
    <button class="ni" id="nav4" onclick="goTo(4)"><span class="sn">4a</span> Bugs da sprint</button>
    <button class="ni" id="nav5" onclick="goTo(5)"><span class="sn">4b</span> Bugs em produção</button>
    <div class="sep"></div>
    <button class="ni" id="nav6" onclick="goTo(6)"><span class="sn">5</span> Tasks não testadas</button>
    <button class="ni" id="nav7" onclick="goTo(7)"><span class="sn">6</span> Riscos</button>
    <button class="ni" id="nav8" onclick="goTo(8)"><span class="sn">7</span> Melhorias</button>
    <div class="sep"></div>
    <button class="ni" id="nav9" onclick="goTo(9)"><span class="sn">↓</span> Exportar</button>
  </nav>

  <div class="main">

    <!-- PAGE 0: DASHBOARD -->
    <div class="page active" id="page0">
      <div class="ph"><h2>Dashboard</h2><p>Visão consolidada da qualidade da sprint</p></div>
      <div class="g4" style="margin-bottom:1rem">
        <div class="dc cp"><div class="dl">Tasks testadas</div><div class="dv vp" id="d-tested">0</div><div class="ds" id="d-tested-sub">de 0 planejadas</div></div>
        <div class="dc cb"><div class="dl">Casos de teste</div><div class="dv vb" id="d-cases">0</div><div class="ds">total executados</div></div>
        <div class="dc co"><div class="dl">Bugs da sprint</div><div class="dv vo" id="d-bsprint">0</div><div class="ds" id="d-bsprint-sub">— críticos / — altos</div></div>
        <div class="dc cr"><div class="dl">Bugs em produção</div><div class="dv vr" id="d-bprod">0</div><div class="ds" id="d-bprod-sub">— críticos / — altos</div></div>
      </div>
      <div class="g3" style="margin-bottom:1rem">
        <div class="dc cg"><div class="dl">Tasks entregues</div><div class="dv vg" id="d-entregues">0</div><div class="ds">concluídas na sprint</div></div>
        <div class="dc cr"><div class="dl">Tasks transbordadas</div><div class="dv vr" id="d-transbordo">0</div><div class="ds">não testadas / bloqueadas</div></div>
        <div class="dc ct2"><div class="dl">Cobertura de testes</div><div class="dv vt" id="d-coverage">0%</div><div class="ds" id="d-coverage-sub">tasks testadas / total</div></div>
      </div>
      <div class="g2" style="margin-bottom:1rem">
        <div class="card" style="margin-bottom:0">
          <div class="ct">Bugs — severidade (sprint + produção)</div>
          <div class="donut-wrap">
            <canvas id="d-sev-chart" width="170" height="170"></canvas>
            <div class="donut-center"><div class="dv" id="d-sev-total">0</div><div class="ds">total bugs</div></div>
          </div>
          <div style="display:flex;flex-wrap:wrap;gap:8px;margin-top:8px" id="d-sev-legend"></div>
        </div>
        <div class="card" style="margin-bottom:0">
          <div class="ct">Bugs — categoria (sprint + produção)</div>
          <div class="chartbox"><canvas id="d-cat-chart"></canvas></div>
        </div>
      </div>
      <div class="card">
        <div class="ct">Distribuição de bugs por BU (sprint + produção)</div>
        <div id="d-bu-breakdown"></div>
      </div>
    </div>

    <!-- PAGE 1 -->
    <div class="page" id="page1">
      <div class="ph"><h2>Informações gerais</h2><p>Dados básicos da sprint e do time</p></div>
      <div class="card">
        <div class="ct">Identificação da sprint</div>
        <div class="g2">
          <div class="f"><label>Número da sprint</label><input type="text" id="sprint-num" placeholder="Ex: Sprint 42" oninput="updateBar();updateDash()"></div>
          <div class="f"><label>QA responsável</label><input type="text" id="qa-resp" placeholder="Nome do QA" oninput="updateBar()"></div>
          <div class="f"><label>Time / Squad</label><input type="text" id="squad" placeholder="Nome do time" oninput="updateBar()"></div>
          <div class="f"><label>Data de publicação</label><input type="date" id="dt-pub" oninput="updateBar()"></div>
          <div class="f"><label>Data de início</label><input type="date" id="dt-inicio" oninput="updateBar()"></div>
          <div class="f"><label>Data de encerramento</label><input type="date" id="dt-fim" oninput="updateBar()"></div>
        </div>
      </div>
      <div class="ab"><button class="btn btnp" onclick="goTo(2)">Próximo →</button></div>
    </div>

    <!-- PAGE 2 -->
    <div class="page" id="page2">
      <div class="ph"><h2>Resumo executivo</h2><p>Panorama geral da qualidade da sprint</p></div>
      <div class="card">
        <div class="ct">Visão geral</div>
        <div class="f"><label>Descreva o que foi entregue, o ciclo de testes e o estado geral de qualidade</label><textarea id="exec-summary" rows="6" placeholder="Ex: A Sprint 42 focou na entrega do módulo de pagamentos..."></textarea></div>
      </div>
      <div class="ab">
        <button class="btn" onclick="goTo(1)">← Anterior</button>
        <button class="btn btnp" onclick="goTo(3)">Próximo →</button>
      </div>
    </div>

    <!-- PAGE 3 -->
    <div class="page" id="page3">
      <div class="ph"><h2>Métricas de testes</h2><p>Tasks e casos de teste da sprint</p></div>
      <div class="card">
        <div class="ct">Total de tasks</div>
        <div class="g2" style="margin-bottom:1rem">
          <div class="f"><label>Total de tasks planejadas</label><input type="number" id="total-tasks" min="0" value="0" oninput="calcTasks()"></div>
          <div class="f"><label>Tasks testadas</label><input type="number" id="tasks-tested" min="0" value="0" oninput="calcTasks()"></div>
        </div>
        <div class="info-row">
          <span>Tasks não testadas: <strong style="color:var(--red);font-family:'DM Mono',monospace" id="tasks-untested-auto">0</strong> <span style="color:var(--text3);font-size:11px">(calculado automaticamente)</span></span>
        </div>
        <div class="f" style="margin-top:1rem"><label>Justificativa por tasks não testadas</label><input type="text" id="untested-reason" placeholder="Ex: Dependência de ambiente, bloqueio de integração..."></div>
      </div>
      <div class="card">
        <div class="ct">Casos de teste por task</div>
        <p style="font-size:13px;color:var(--text2);margin-bottom:1rem">Adicione as tasks e a quantidade de casos. O total atualiza automaticamente.</p>
        <div style="margin-bottom:1rem"><div class="tb"><span>Total de casos de teste:</span><span class="tbn" id="total-cases-badge">0</span></div></div>
        <div style="overflow-x:auto">
          <table class="tbl">
            <thead><tr><th style="min-width:200px">Nome da task</th><th style="min-width:110px;text-align:center">Qtd. de casos</th><th style="min-width:130px">Status da task</th><th style="width:36px"></th></tr></thead>
            <tbody id="task-cases-body"><tr><td colspan="4" style="padding:12px;font-size:13px;color:var(--text3)">Nenhuma task adicionada ainda.</td></tr></tbody>
          </table>
        </div>
        <button class="btn btns" style="margin-top:10px" onclick="addTaskCase()">+ Adicionar task</button>
      </div>
      <div class="ab">
        <button class="btn" onclick="goTo(2)">← Anterior</button>
        <button class="btn btnp" onclick="goTo(4)">Próximo →</button>
      </div>
    </div>

    <!-- PAGE 4 -->
    <div class="page" id="page4">
      <div class="ph"><h2>Bugs da sprint</h2><p>Bugs encontrados durante o ciclo de testes</p></div>
      <div class="card">
        <div style="display:flex;flex-wrap:wrap;gap:10px;align-items:center;margin-bottom:1rem">
          <div class="tb"><span>Total bugs sprint:</span><span class="tbn" id="sprint-bugs-count">0</span></div>
          <div class="counts-row">
            <span><span style="color:var(--red);font-family:'DM Mono',monospace;font-weight:700" id="sc-crit">0</span> <span style="color:var(--text2)">Crítico</span></span>
            <span style="color:var(--border2)">·</span>
            <span><span style="color:var(--orange);font-family:'DM Mono',monospace;font-weight:700" id="sc-alto">0</span> <span style="color:var(--text2)">Alto</span></span>
            <span style="color:var(--border2)">·</span>
            <span><span style="color:var(--amber);font-family:'DM Mono',monospace;font-weight:700" id="sc-med">0</span> <span style="color:var(--text2)">Médio</span></span>
            <span style="color:var(--border2)">·</span>
            <span><span style="color:var(--green);font-family:'DM Mono',monospace;font-weight:700" id="sc-baixo">0</span> <span style="color:var(--text2)">Baixo</span></span>
          </div>
          <div class="fixed-row">Corrigidos: <span style="color:var(--green);font-family:'DM Mono',monospace;font-size:16px;font-weight:700" id="sc-fixed">0</span> <span style="color:var(--text3);font-size:11px">(automático)</span></div>
        </div>
        <div style="overflow-x:auto">
          <table class="tbl">
            <thead><tr><th style="min-width:160px">BU / Produto</th><th style="min-width:140px">Breve Descrição</th><th style="min-width:110px">Responsável</th><th style="min-width:120px">Categoria</th><th style="min-width:100px">Criticidade</th><th style="min-width:110px">Status</th><th style="width:36px"></th></tr></thead>
            <tbody id="sprint-bug-body"><tr><td colspan="7" style="padding:12px;font-size:13px;color:var(--text3)">Nenhum bug adicionado.</td></tr></tbody>
          </table>
        </div>
        <button class="btn btns" style="margin-top:10px" onclick="addSprintBug()">+ Adicionar bug</button>
      </div>
      <div class="ab">
        <button class="btn" onclick="goTo(3)">← Anterior</button>
        <button class="btn btnp" onclick="goTo(5)">Próximo →</button>
      </div>
    </div>

    <!-- PAGE 5 -->
    <div class="page" id="page5">
      <div class="ph"><h2>Bugs em produção / Incidentes</h2><p>Incidentes identificados em ambiente de produção</p></div>
      <div class="card">
        <div style="display:flex;flex-wrap:wrap;gap:10px;align-items:center;margin-bottom:1rem">
          <div class="tb" style="border-color:rgba(248,113,113,.3);background:var(--red-bg)"><span style="color:var(--text2)">Total bugs produção:</span><span class="tbn" style="color:var(--red)" id="prod-bugs-count">0</span></div>
          <div class="counts-row">
            <span><span style="color:var(--red);font-family:'DM Mono',monospace;font-weight:700" id="pc-crit">0</span> <span style="color:var(--text2)">Crítico</span></span>
            <span style="color:var(--border2)">·</span>
            <span><span style="color:var(--orange);font-family:'DM Mono',monospace;font-weight:700" id="pc-alto">0</span> <span style="color:var(--text2)">Alto</span></span>
            <span style="color:var(--border2)">·</span>
            <span><span style="color:var(--amber);font-family:'DM Mono',monospace;font-weight:700" id="pc-med">0</span> <span style="color:var(--text2)">Médio</span></span>
            <span style="color:var(--border2)">·</span>
            <span><span style="color:var(--green);font-family:'DM Mono',monospace;font-weight:700" id="pc-baixo">0</span> <span style="color:var(--text2)">Baixo</span></span>
          </div>
          <div class="fixed-row">Corrigidos: <span style="color:var(--green);font-family:'DM Mono',monospace;font-size:16px;font-weight:700" id="pc-fixed">0</span> <span style="color:var(--text3);font-size:11px">(automático)</span></div>
        </div>
        <div style="overflow-x:auto">
          <table class="tbl">
            <thead><tr><th style="min-width:160px">BU / Produto</th><th style="min-width:140px">Breve Descrição</th><th style="min-width:110px">Responsável</th><th style="min-width:120px">Categoria</th><th style="min-width:100px">Criticidade</th><th style="min-width:110px">Status</th><th style="width:36px"></th></tr></thead>
            <tbody id="prod-bug-body"><tr><td colspan="7" style="padding:12px;font-size:13px;color:var(--text3)">Nenhum incidente adicionado.</td></tr></tbody>
          </table>
        </div>
        <button class="btn btns" style="margin-top:10px" onclick="addProdBug()">+ Adicionar incidente</button>
      </div>
      <div class="ab">
        <button class="btn" onclick="goTo(4)">← Anterior</button>
        <button class="btn btnp" onclick="goTo(6)">Próximo →</button>
      </div>
    </div>

    <!-- PAGE 6 -->
    <div class="page" id="page6">
      <div class="ph"><h2>Tasks não testadas / bloqueadas</h2><p>Registros aqui alimentam o indicador de transbordamento no dashboard.</p></div>
      <div class="card">
        <div class="ct">Registro de bloqueios</div>
        <div id="blocked-list"><p style="font-size:13px;color:var(--text3);padding:8px 0">Nenhuma task registrada.</p></div>
        <button class="btn btns" style="margin-top:10px" onclick="addBlockedRow()">+ Adicionar task</button>
      </div>
      <div class="ab">
        <button class="btn" onclick="goTo(5)">← Anterior</button>
        <button class="btn btnp" onclick="goTo(7)">Próximo →</button>
      </div>
    </div>

    <!-- PAGE 7 -->
    <div class="page" id="page7">
      <div class="ph"><h2>Pontos de atenção e riscos</h2><p>Riscos de qualidade para a próxima sprint</p></div>
      <div class="card">
        <div class="ct">Riscos identificados</div>
        <div id="risks-list"><p style="font-size:13px;color:var(--text3);padding:8px 0">Nenhum risco registrado.</p></div>
        <button class="btn btns" style="margin-top:10px" onclick="addRisk()">+ Adicionar risco</button>
      </div>
      <div class="ab">
        <button class="btn" onclick="goTo(6)">← Anterior</button>
        <button class="btn btnp" onclick="goTo(8)">Próximo →</button>
      </div>
    </div>

    <!-- PAGE 8 -->
    <div class="page" id="page8">
      <div class="ph"><h2>Melhorias de processo</h2><p>Sugestões identificadas durante a sprint</p></div>
      <div class="card">
        <div class="ct">Sugestões de melhoria</div>
        <div id="improvements-list"><p style="font-size:13px;color:var(--text3);padding:8px 0">Nenhuma melhoria registrada.</p></div>
        <button class="btn btns" style="margin-top:10px" onclick="addImprovement()">+ Adicionar melhoria</button>
      </div>
      <div class="ab">
        <button class="btn" onclick="goTo(7)">← Anterior</button>
        <button class="btn btnp" onclick="goTo(9)">Ver relatório →</button>
      </div>
    </div>

    <!-- PAGE 9 -->
    <div class="page" id="page9">
      <div class="ph"><h2>Exportar relatório</h2><p>Prévia completa do QA Review</p></div>
      <div class="card">
        <div class="ct">Prévia do relatório</div>
        <div class="expbox" id="preview-content"></div>
        <div style="display:flex;gap:8px;margin-top:1rem;flex-wrap:wrap">
          <button class="btn btnp" onclick="copyReport()">📋 Copiar relatório</button>
          <button class="btn" onclick="window.print()">🖨️ Imprimir / PDF</button>
          <button class="btn" onclick="saveJSON()" style="border-color:rgba(62,207,142,.4);color:var(--green)">💾 Salvar dados (.json)</button>
          <button class="btn" onclick="loadJSON()" style="border-color:rgba(124,106,247,.4);color:var(--accent2)">📂 Carregar dados (.json)</button>
        </div>
      </div>
      <div class="ab"><button class="btn" onclick="goTo(8)">← Anterior</button></div>
    </div>

  </div>
</div>

<script>
var currentPage = 0;
var taskCases = [];
var sprintBugs = [];
var prodBugs = [];
var blockedRows = [];
var riskRows = [];
var improveRows = [];
var CRIT_OPT = ['Crítico','Alto','Médio','Baixo'];
var BUG_STATUS = ['Aberto','Corrigido','Cancelado'];
var SEV_COLORS = {'Crítico':'#f87171','Alto':'#fb923c','Médio':'#fbbf24','Baixo':'#3ecf8e'};
var dashSevChart = null;
var dashCatChart = null;

function goTo(n) {
  document.getElementById('page'+currentPage).classList.remove('active');
  document.getElementById('nav'+currentPage).classList.remove('active');
  currentPage = n;
  document.getElementById('page'+n).classList.add('active');
  document.getElementById('nav'+n).classList.add('active');
  if (n === 3) renderTaskCases();
  if (n === 4) renderSprintBugs();
  if (n === 5) renderProdBugs();
  if (n === 6) renderBlockedList();
  if (n === 7) renderRiskList();
  if (n === 8) renderImproveList();
  if (n === 9) buildPreview();
  if (n === 0) { setTimeout(updateDash, 80); }
  window.scrollTo(0,0);
}

function updateBar() {
  var s = val('sprint-num'), q = val('qa-resp'), sq = val('squad');
  var di = val('dt-inicio'), df = val('dt-fim');
  setTxt('bar-sprint', s || 'Sprint —');
  setTxt('bar-qa', q || '—');
  setTxt('bar-squad', sq || '—');
  setTxt('bar-period', (di && df) ? di + ' → ' + df : '—');
}

function val(id) { var e = document.getElementById(id); return e ? e.value : ''; }
function num(id) { return parseFloat(val(id)) || 0; }
function setTxt(id, v) { var e = document.getElementById(id); if (e) e.textContent = v; }
function esc(s) { return (s||'').replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }

function calcTasks() {
  var total = num('total-tasks');
  var tested = num('tasks-tested');
  var untested = total - tested;
  if (untested < 0) untested = 0;
  setTxt('tasks-untested-auto', untested);
  updateDash();
}

function getTotalCases() {
  var t = 0;
  for (var i = 0; i < taskCases.length; i++) t += parseInt(taskCases[i].cases) || 0;
  return t;
}

function renderTaskCases() {
  var tbody = document.getElementById('task-cases-body');
  if (!tbody) return;
  if (!taskCases.length) {
    tbody.innerHTML = '<tr><td colspan="4" style="padding:12px;font-size:13px;color:var(--text3)">Nenhuma task adicionada ainda.</td></tr>';
    updateCasesBadge(); return;
  }
  var html = '';
  for (var i = 0; i < taskCases.length; i++) {
    var t = taskCases[i];
    var optsStatus = '<option value=""' + (!t.status ? ' selected' : '') + '>— selecione —</option>';
    var statusList = ['Backlog','Prioritized','Doing','Review','Validation','Deployment','Done'];
    for (var s = 0; s < statusList.length; s++) {
      optsStatus += '<option' + (t.status === statusList[s] ? ' selected' : '') + '>' + statusList[s] + '</option>';
    }
    html += '<tr>';
    html += '<td><input type="text" value="' + esc(t.name) + '" placeholder="Nome da task / US" data-i="' + i + '" data-f="name" class="tc-name" oninput="tcUpdate(this)"></td>';
    html += '<td style="text-align:center"><input type="number" min="0" value="' + (t.cases||0) + '" style="font-family:DM Mono,monospace;text-align:center;max-width:80px;margin:0 auto;display:block" data-i="' + i + '" class="tc-cases" oninput="tcCases(this)"></td>';
    html += '<td><select data-i="' + i + '" class="tc-status" onchange="tcStatus(this)">' + optsStatus + '</select></td>';
    html += '<td><button class="btn btnd" data-i="' + i + '" onclick="removeTask(this)">✕</button></td>';
    html += '</tr>';
  }
  tbody.innerHTML = html;
  updateCasesBadge();
}

function tcUpdate(el) { taskCases[el.dataset.i].name = el.value; }
function tcCases(el) { taskCases[el.dataset.i].cases = el.value; updateCasesBadge(); }
function tcStatus(el) { taskCases[el.dataset.i].status = el.value; updateCasesBadge(); }
function removeTask(el) { taskCases.splice(el.dataset.i, 1); renderTaskCases(); }

function updateCasesBadge() {
  setTxt('total-cases-badge', getTotalCases());
  updateDash();
}

function addTaskCase() {
  taskCases.push({name:'', cases:0, status:''});
  renderTaskCases();
}

function cntCrit(arr, c) {
  var n = 0;
  for (var i = 0; i < arr.length; i++) if (arr[i].crit === c) n++;
  return n;
}
function cntStatus(arr, s) {
  var n = 0;
  for (var i = 0; i < arr.length; i++) if (arr[i].status === s) n++;
  return n;
}

function makeBugRow(bug, i, prefix) {
  var optsCrit = '', optsStatus = '';
  for (var c = 0; c < CRIT_OPT.length; c++) {
    optsCrit += '<option' + (bug.crit === CRIT_OPT[c] ? ' selected' : '') + '>' + CRIT_OPT[c] + '</option>';
  }
  for (var s = 0; s < BUG_STATUS.length; s++) {
    optsStatus += '<option' + (bug.status === BUG_STATUS[s] ? ' selected' : '') + '>' + BUG_STATUS[s] + '</option>';
  }
  var row = '<tr>';
  row += '<td><input type="text" value="' + esc(bug.bu) + '" placeholder="Ex: Mobile — GL Activation" data-p="' + prefix + '" data-i="' + i + '" data-f="bu" class="bug-txt" oninput="bugTxt(this)"></td>';
  row += '<td><input type="text" value="' + esc(bug.func) + '" placeholder="Breve descrição do bug" data-p="' + prefix + '" data-i="' + i + '" data-f="func" class="bug-txt" oninput="bugTxt(this)"></td>';
  row += '<td><input type="text" value="' + esc(bug.resp) + '" placeholder="Nome" data-p="' + prefix + '" data-i="' + i + '" data-f="resp" class="bug-txt" oninput="bugTxt(this)"></td>';
  row += '<td><input type="text" value="' + esc(bug.cat) + '" placeholder="Ex: Funcionalidade" data-p="' + prefix + '" data-i="' + i + '" data-f="cat" class="bug-txt" oninput="bugTxt(this)"></td>';
  row += '<td><select data-p="' + prefix + '" data-i="' + i + '" data-f="crit" class="bug-sel" onchange="bugSel(this)">' + optsCrit + '</select></td>';
  row += '<td><select data-p="' + prefix + '" data-i="' + i + '" data-f="status" class="bug-sel" onchange="bugSel(this)">' + optsStatus + '</select></td>';
  row += '<td><button class="btn btnd" data-p="' + prefix + '" data-i="' + i + '" onclick="removeBug(this)">✕</button></td>';
  row += '</tr>';
  return row;
}

function bugTxt(el) {
  var arr = el.dataset.p === 'sprint' ? sprintBugs : prodBugs;
  arr[el.dataset.i][el.dataset.f] = el.value;
  if (el.dataset.f === 'cat') updateBugCounts();
}
function bugSel(el) {
  var arr = el.dataset.p === 'sprint' ? sprintBugs : prodBugs;
  arr[el.dataset.i][el.dataset.f] = el.value;
  updateBugCounts();
}
function removeBug(el) {
  if (el.dataset.p === 'sprint') { sprintBugs.splice(el.dataset.i, 1); renderSprintBugs(); }
  else { prodBugs.splice(el.dataset.i, 1); renderProdBugs(); }
}

function renderSprintBugs() {
  var tbody = document.getElementById('sprint-bug-body');
  if (!tbody) return;
  if (!sprintBugs.length) { tbody.innerHTML = '<tr><td colspan="7" style="padding:12px;font-size:13px;color:var(--text3)">Nenhum bug adicionado.</td></tr>'; updateBugCounts(); return; }
  var html = '';
  for (var i = 0; i < sprintBugs.length; i++) html += makeBugRow(sprintBugs[i], i, 'sprint');
  tbody.innerHTML = html;
  updateBugCounts();
}

function renderProdBugs() {
  var tbody = document.getElementById('prod-bug-body');
  if (!tbody) return;
  if (!prodBugs.length) { tbody.innerHTML = '<tr><td colspan="7" style="padding:12px;font-size:13px;color:var(--text3)">Nenhum incidente adicionado.</td></tr>'; updateBugCounts(); return; }
  var html = '';
  for (var i = 0; i < prodBugs.length; i++) html += makeBugRow(prodBugs[i], i, 'prod');
  tbody.innerHTML = html;
  updateBugCounts();
}

function addSprintBug() { sprintBugs.push({bu:'',func:'',resp:'',cat:'',crit:'Alto',status:'Aberto'}); renderSprintBugs(); }
function addProdBug()   { prodBugs.push({bu:'',func:'',resp:'',cat:'',crit:'Crítico',status:'Aberto'}); renderProdBugs(); }

function updateBugCounts() {
  var st = sprintBugs.length;
  setTxt('sprint-bugs-count', st);
  setTxt('sc-crit', cntCrit(sprintBugs,'Crítico'));
  setTxt('sc-alto', cntCrit(sprintBugs,'Alto'));
  setTxt('sc-med',  cntCrit(sprintBugs,'Médio'));
  setTxt('sc-baixo',cntCrit(sprintBugs,'Baixo'));
  setTxt('sc-fixed',cntStatus(sprintBugs,'Corrigido'));
  var pt = prodBugs.length;
  setTxt('prod-bugs-count', pt);
  setTxt('pc-crit', cntCrit(prodBugs,'Crítico'));
  setTxt('pc-alto', cntCrit(prodBugs,'Alto'));
  setTxt('pc-med',  cntCrit(prodBugs,'Médio'));
  setTxt('pc-baixo',cntCrit(prodBugs,'Baixo'));
  setTxt('pc-fixed',cntStatus(prodBugs,'Corrigido'));
  updateDash();
}

function renderBlockedList() {
  var el = document.getElementById('blocked-list');
  if (!el) return;
  if (!blockedRows.length) { el.innerHTML = '<p style="font-size:13px;color:var(--text3);padding:8px 0">Nenhuma task registrada.</p>'; updateDash(); return; }
  var html = '';
  for (var i = 0; i < blockedRows.length; i++) {
    var r = blockedRows[i];
    html += '<div class="blocked-item">';
    html += '<div class="g2" style="gap:10px;margin-bottom:10px">';
    html += '<div><label>Task / Link</label><input type="text" value="' + esc(r.item) + '" placeholder="Nome ou link da task" data-i="' + i + '" data-f="item" class="bl-txt" oninput="blTxt(this)"></div>';
    html += '<div><label>Motivo do bloqueio</label><input type="text" value="' + esc(r.motivo) + '" placeholder="Ex: Ambiente indisponível" data-i="' + i + '" data-f="motivo" class="bl-txt" oninput="blTxt(this)"></div>';
    html += '</div>';
    html += '<div style="display:flex;align-items:center;justify-content:space-between">';
    html += '<div style="display:flex;align-items:center;gap:8px"><span style="font-size:12px;color:var(--text2)">Risco:</span>';
    html += '<button class="rb' + (r.risco==='Alto'?' ra':'') + '" data-i="' + i + '" data-v="Alto" onclick="blRisco(this)">Alto</button>';
    html += '<button class="rb' + (r.risco==='Médio'?' rm':'') + '" data-i="' + i + '" data-v="Médio" onclick="blRisco(this)">Médio</button>';
    html += '<button class="rb' + (r.risco==='Baixo'?' rl':'') + '" data-i="' + i + '" data-v="Baixo" onclick="blRisco(this)">Baixo</button>';
    html += '</div>';
    html += '<button class="btn btnd" data-i="' + i + '" onclick="removeBlocked(this)">✕</button>';
    html += '</div></div>';
  }
  el.innerHTML = html;
  updateDash();
}

function blTxt(el) { blockedRows[el.dataset.i][el.dataset.f] = el.value; }
function blRisco(el) { blockedRows[el.dataset.i].risco = el.dataset.v; renderBlockedList(); }
function removeBlocked(el) { blockedRows.splice(el.dataset.i, 1); renderBlockedList(); }
function addBlockedRow() { blockedRows.push({item:'',motivo:'',risco:''}); renderBlockedList(); }

function renderRiskList() {
  var el = document.getElementById('risks-list');
  if (!el) return;
  if (!riskRows.length) { el.innerHTML = '<p style="font-size:13px;color:var(--text3);padding:8px 0">Nenhum risco registrado.</p>'; return; }
  var html = '';
  for (var i = 0; i < riskRows.length; i++) {
    html += '<div class="ti"><span style="margin-top:10px;font-size:16px;flex-shrink:0">⚠️</span>';
    html += '<input type="text" value="' + esc(riskRows[i]) + '" placeholder="Descreva o risco" data-i="' + i + '" class="rk-txt" oninput="rkTxt(this)" style="flex:1">';
    html += '<button class="btn btnd" data-i="' + i + '" onclick="removeRisk(this)">✕</button></div>';
  }
  el.innerHTML = html;
}
function rkTxt(el) { riskRows[el.dataset.i] = el.value; }
function removeRisk(el) { riskRows.splice(el.dataset.i, 1); renderRiskList(); }
function addRisk() { riskRows.push(''); renderRiskList(); }

function renderImproveList() {
  var el = document.getElementById('improvements-list');
  if (!el) return;
  if (!improveRows.length) { el.innerHTML = '<p style="font-size:13px;color:var(--text3);padding:8px 0">Nenhuma melhoria registrada.</p>'; return; }
  var html = '';
  for (var i = 0; i < improveRows.length; i++) {
    html += '<div class="ti"><span style="margin-top:10px;font-size:16px;flex-shrink:0">💡</span>';
    html += '<input type="text" value="' + esc(improveRows[i]) + '" placeholder="Descreva a melhoria" data-i="' + i + '" class="im-txt" oninput="imTxt(this)" style="flex:1">';
    html += '<button class="btn btnd" data-i="' + i + '" onclick="removeImprove(this)">✕</button></div>';
  }
  el.innerHTML = html;
}
function imTxt(el) { improveRows[el.dataset.i] = el.value; }
function removeImprove(el) { improveRows.splice(el.dataset.i, 1); renderImproveList(); }
function addImprovement() { improveRows.push(''); renderImproveList(); }

function updateDash() {
  var totalTasks = num('total-tasks');
  var testedTasks = num('tasks-tested');
  var pct = totalTasks > 0 ? Math.round(testedTasks / totalTasks * 100) : 0;
  var totalCases = getTotalCases();
  var st = sprintBugs.length, pt = prodBugs.length;
  var sc = cntCrit(sprintBugs,'Crítico'), sa = cntCrit(sprintBugs,'Alto');
  var pc = cntCrit(prodBugs,'Crítico'), pa = cntCrit(prodBugs,'Alto');

  var entregues = 0;
  for (var i = 0; i < taskCases.length; i++) if (taskCases[i].status === 'Done') entregues++;
  var transbordo = blockedRows.length;
  for (var i = 0; i < taskCases.length; i++) if (!taskCases[i].status || taskCases[i].status === 'Backlog') transbordo++;

  setTxt('d-tested', testedTasks);
  setTxt('d-tested-sub', 'de ' + totalTasks + ' planejadas');
  setTxt('d-cases', totalCases);
  setTxt('d-bsprint', st);
  setTxt('d-bsprint-sub', sc + ' críticos / ' + sa + ' altos');
  setTxt('d-bprod', pt);
  setTxt('d-bprod-sub', pc + ' críticos / ' + pa + ' altos');
  setTxt('d-entregues', entregues);
  setTxt('d-transbordo', transbordo);
  setTxt('d-coverage', pct + '%');
  setTxt('d-coverage-sub', testedTasks + ' testadas de ' + totalTasks);

  // combined bugs for charts
  var allBugs = sprintBugs.concat(prodBugs);
  var sevVals = [
    cntCrit(allBugs,'Crítico'),
    cntCrit(allBugs,'Alto'),
    cntCrit(allBugs,'Médio'),
    cntCrit(allBugs,'Baixo')
  ];
  var sevLabels = ['Crítico','Alto','Médio','Baixo'];
  var sevColors = ['#f87171','#fb923c','#fbbf24','#3ecf8e'];
  setTxt('d-sev-total', allBugs.length);

  if (!dashSevChart) {
    var ctx = document.getElementById('d-sev-chart');
    if (ctx) {
      dashSevChart = new Chart(ctx, {
        type: 'doughnut',
        data: { labels: sevLabels, datasets: [{ data: sevVals, backgroundColor: sevColors, borderWidth: 0, hoverOffset: 6 }] },
        options: { responsive: false, cutout: '68%', plugins: { legend: { display: false } } }
      });
    }
  } else {
    dashSevChart.data.datasets[0].data = sevVals;
    dashSevChart.update();
  }

  var leg = document.getElementById('d-sev-legend');
  if (leg) {
    var lhtml = '';
    for (var i = 0; i < sevLabels.length; i++) {
      lhtml += '<div style="display:flex;align-items:center;gap:5px;font-size:11px;color:var(--text2)">';
      lhtml += '<div style="width:8px;height:8px;border-radius:50%;background:' + sevColors[i] + '"></div>';
      lhtml += sevLabels[i] + ': <span style="color:' + sevColors[i] + ';font-weight:600">' + sevVals[i] + '</span></div>';
    }
    leg.innerHTML = lhtml;
  }

  // category chart
  var catMap = {};
  for (var i = 0; i < allBugs.length; i++) {
    var k = allBugs[i].cat || 'Outro';
    if (!k) k = 'Outro';
    catMap[k] = (catMap[k] || 0) + 1;
  }
  var catLabels = Object.keys(catMap);
  var catData = [];
  for (var i = 0; i < catLabels.length; i++) catData.push(catMap[catLabels[i]]);
  var catColors = ['#60a5fa','#c084fc','#3ecf8e','#fb923c','#f87171','#fbbf24','#a78bfa','#34d399','#f472b6'];

  if (!dashCatChart) {
    var ctx2 = document.getElementById('d-cat-chart');
    if (ctx2) {
      dashCatChart = new Chart(ctx2, {
        type: 'bar',
        data: {
          labels: catLabels.length ? catLabels : ['—'],
          datasets: [{ data: catData.length ? catData : [0], backgroundColor: catColors, borderRadius: 6, borderWidth: 0 }]
        },
        options: {
          responsive: true, maintainAspectRatio: false,
          plugins: { legend: { display: false } },
          scales: {
            y: { beginAtZero: true, ticks: { color:'#9898a8', font:{size:11,family:'DM Mono'}, stepSize:1 }, grid: { color:'rgba(255,255,255,0.06)' } },
            x: { grid: { display: false }, ticks: { color:'#9898a8', font:{size:11} } }
          }
        }
      });
    }
  } else {
    dashCatChart.data.labels = catLabels.length ? catLabels : ['—'];
    dashCatChart.data.datasets[0].data = catData.length ? catData : [0];
    dashCatChart.data.datasets[0].backgroundColor = catColors;
    dashCatChart.update();
  }

  // bugs by BU breakdown
  var buEl = document.getElementById('d-bu-breakdown');
  if (buEl) {
    var buMap = {};
    var allBugs2 = sprintBugs.concat(prodBugs);
    for (var i = 0; i < allBugs2.length; i++) {
      var bk = (allBugs2[i].bu || '').trim() || 'Não especificado';
      buMap[bk] = (buMap[bk] || 0) + 1;
    }
    var buKeys = Object.keys(buMap);
    var buColors2 = ['#7c6af7','#60a5fa','#3ecf8e','#fb923c','#f87171','#fbbf24','#c084fc','#2dd4bf','#f472b6','#34d399'];
    if (!buKeys.length) {
      buEl.innerHTML = '<p style="font-size:13px;color:var(--text3);padding:8px 0">Nenhum bug registrado ainda.</p>';
    } else {
      var buTotal = allBugs2.length || 1;
      var bhtml = '';
      for (var i = 0; i < buKeys.length; i++) {
        var bPct = Math.round(buMap[buKeys[i]] / buTotal * 100);
        var bColor = buColors2[i % buColors2.length];
        bhtml +=
          '<div class="barrow">' +
            '<span style="font-size:12px;color:var(--text2);width:160px;flex-shrink:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap" title="' + esc(buKeys[i]) + '">' + esc(buKeys[i]) + '</span>' +
            '<div class="barbg"><div class="barfill" style="background:' + bColor + ';width:' + bPct + '%"></div></div>' +
            '<span style="font-size:13px;font-weight:600;font-family:DM Mono,monospace;color:' + bColor + ';min-width:60px;text-align:right">' + buMap[buKeys[i]] + ' <span style="font-size:10px;font-weight:400;color:var(--text3)">(' + bPct + '%)</span></span>' +
          '</div>';
      }
      buEl.innerHTML = bhtml;
    }
  }
}

function buildPreview() {
  var sprint = val('sprint-num') || '—';
  var totalTasks = num('total-tasks'), testedTasks = num('tasks-tested');
  var untestedTasks = totalTasks - testedTasks; if (untestedTasks < 0) untestedTasks = 0;
  var pct = totalTasks > 0 ? Math.round(testedTasks / totalTasks * 100) : 0;
  var totalCases = getTotalCases();
  var st = sprintBugs.length, pt = prodBugs.length;
  var sf = cntStatus(sprintBugs,'Corrigido'), pf = cntStatus(prodBugs,'Corrigido');
  var entregues = 0, transbordo = blockedRows.length;
  for (var i = 0; i < taskCases.length; i++) {
    if (taskCases[i].status === 'Done') entregues++;
    else if (!taskCases[i].status || taskCases[i].status === 'Backlog') transbordo++;
  }

  var taskLines = '';
  for (var i = 0; i < taskCases.length; i++) {
    taskLines += '  • ' + (taskCases[i].name || '(sem nome)') + ' — ' + (taskCases[i].cases || 0) + ' casos — ' + taskCases[i].status + '\n';
  }
  if (!taskLines) taskLines = '  Nenhuma task registrada.\n';

  var sbLines = '';
  for (var i = 0; i < sprintBugs.length; i++) {
    var b = sprintBugs[i];
    sbLines += '  • [' + b.crit + '] ' + (b.bu||'—') + ' | ' + (b.func||'—') + ' | Resp: ' + (b.resp||'—') + ' | Cat: ' + (b.cat||'—') + ' | ' + b.status + '\n';
  }
  if (!sbLines) sbLines = '  Nenhum bug registrado.\n';

  var pbLines = '';
  for (var i = 0; i < prodBugs.length; i++) {
    var b = prodBugs[i];
    pbLines += '  • [' + b.crit + '] ' + (b.bu||'—') + ' | ' + (b.func||'—') + ' | Resp: ' + (b.resp||'—') + ' | Cat: ' + (b.cat||'—') + ' | ' + b.status + '\n';
  }
  if (!pbLines) pbLines = '  Nenhum incidente registrado.\n';

  var blLines = '';
  for (var i = 0; i < blockedRows.length; i++) {
    var r = blockedRows[i];
    blLines += '  • ' + (r.item||'(sem item)') + ' | Motivo: ' + (r.motivo||'—') + ' | Risco: ' + (r.risco||'—') + '\n';
  }
  if (!blLines) blLines = '  Nenhuma task bloqueada.\n';

  var rkLines = '';
  for (var i = 0; i < riskRows.length; i++) rkLines += '  ⚠️  ' + (riskRows[i]||'—') + '\n';
  if (!rkLines) rkLines = '  Nenhum risco registrado.\n';

  var imLines = '';
  for (var i = 0; i < improveRows.length; i++) imLines += '  💡 ' + (improveRows[i]||'—') + '\n';
  if (!imLines) imLines = '  Nenhuma melhoria registrada.\n';

  var report =
    'QA REVIEW — ' + sprint + '\n' +
    '━'.repeat(54) + '\n' +
    'Período: ' + (val('dt-inicio')||'—') + ' → ' + (val('dt-fim')||'—') + '\n' +
    'QA Responsável: ' + (val('qa-resp')||'—') + '\n' +
    'Time / Squad: ' + (val('squad')||'—') + '\n' +
    'Publicação: ' + (val('dt-pub')||'—') + '\n\n' +
    '━━━ RESUMO EXECUTIVO ━━━\n' + (val('exec-summary')||'—') + '\n\n' +
    '━━━ MÉTRICAS DE TESTES ━━━\n' +
    'Total de tasks planejadas: ' + totalTasks + '\n' +
    'Tasks testadas: ' + testedTasks + '  |  Não testadas: ' + untestedTasks + '\n' +
    'Cobertura: ' + pct + '%\n' +
    'Justificativa: ' + (val('untested-reason')||'—') + '\n' +
    'Tasks entregues: ' + entregues + '  |  Transbordadas: ' + transbordo + '\n' +
    'Total de casos de teste: ' + totalCases + '\n' +
    taskLines + '\n' +
    '━━━ BUGS DA SPRINT ━━━\n' +
    'Total: ' + st + '  |  Corrigidos: ' + sf + '\n' +
    '🔴 Crítico: ' + cntCrit(sprintBugs,'Crítico') + '  🟠 Alto: ' + cntCrit(sprintBugs,'Alto') + '  🟡 Médio: ' + cntCrit(sprintBugs,'Médio') + '  🟢 Baixo: ' + cntCrit(sprintBugs,'Baixo') + '\n' +
    sbLines + '\n' +
    '━━━ BUGS EM PRODUÇÃO / INCIDENTES ━━━\n' +
    'Total: ' + pt + '  |  Corrigidos: ' + pf + '\n' +
    '🔴 Crítico: ' + cntCrit(prodBugs,'Crítico') + '  🟠 Alto: ' + cntCrit(prodBugs,'Alto') + '  🟡 Médio: ' + cntCrit(prodBugs,'Médio') + '  🟢 Baixo: ' + cntCrit(prodBugs,'Baixo') + '\n' +
    pbLines + '\n' +
    '━━━ TASKS NÃO TESTADAS / BLOQUEADAS ━━━\n' +
    blLines + '\n' +
    '━━━ RISCOS E PONTOS DE ATENÇÃO ━━━\n' +
    rkLines + '\n' +
    '━━━ MELHORIAS DE PROCESSO ━━━\n' +
    imLines + '\n' +
    '─'.repeat(54) + '\n' +
    'Gerado pelo QA Review Dashboard';

  document.getElementById('preview-content').textContent = report;
}

function copyReport() {
  var text = document.getElementById('preview-content').textContent;
  navigator.clipboard.writeText(text).then(function() {
    var btn = document.querySelector('.btnp');
    var orig = btn.textContent;
    btn.textContent = '✅ Copiado!';
    setTimeout(function(){ btn.textContent = orig; }, 2000);
  });
}

/* ── SAVE / LOAD / LOCALSTORAGE ── */
var LS_KEY = 'qa_review_data';

function collectData() {
  return {
    sprintNum:      val('sprint-num'),
    qaResp:         val('qa-resp'),
    squad:          val('squad'),
    dtPub:          val('dt-pub'),
    dtInicio:       val('dt-inicio'),
    dtFim:          val('dt-fim'),
    execSummary:    val('exec-summary'),
    totalTasks:     val('total-tasks'),
    tasksTested:    val('tasks-tested'),
    untestedReason: val('untested-reason'),
    taskCases:      taskCases,
    sprintBugs:     sprintBugs,
    prodBugs:       prodBugs,
    blockedRows:    blockedRows,
    riskRows:       riskRows,
    improveRows:    improveRows
  };
}

function applyData(d) {
  if (!d) return;
  function setVal(id, v) { var e = document.getElementById(id); if (e && v !== undefined) e.value = v; }
  setVal('sprint-num',     d.sprintNum);
  setVal('qa-resp',        d.qaResp);
  setVal('squad',          d.squad);
  setVal('dt-pub',         d.dtPub);
  setVal('dt-inicio',      d.dtInicio);
  setVal('dt-fim',         d.dtFim);
  setVal('exec-summary',   d.execSummary);
  setVal('total-tasks',    d.totalTasks);
  setVal('tasks-tested',   d.tasksTested);
  setVal('untested-reason',d.untestedReason);
  if (Array.isArray(d.taskCases))   taskCases   = d.taskCases;
  if (Array.isArray(d.sprintBugs))  sprintBugs  = d.sprintBugs;
  if (Array.isArray(d.prodBugs))    prodBugs    = d.prodBugs;
  if (Array.isArray(d.blockedRows)) blockedRows = d.blockedRows;
  if (Array.isArray(d.riskRows))    riskRows    = d.riskRows;
  if (Array.isArray(d.improveRows)) improveRows = d.improveRows;
  updateBar();
  calcTasks();
  updateBugCounts();
  updateDash();
}

function autoSave() {
  try { localStorage.setItem(LS_KEY, JSON.stringify(collectData())); } catch(e) {}
}

function autoLoad() {
  try {
    var raw = localStorage.getItem(LS_KEY);
    if (raw) applyData(JSON.parse(raw));
  } catch(e) {}
}

function saveJSON() {
  var data = JSON.stringify(collectData(), null, 2);
  var blob = new Blob([data], {type: 'application/json'});
  var a = document.createElement('a');
  a.href = URL.createObjectURL(blob);
  a.download = 'qa-review-dados.json';
  a.click();
  URL.revokeObjectURL(a.href);
}

function loadJSON() {
  var input = document.createElement('input');
  input.type = 'file';
  input.accept = '.json,application/json';
  input.onchange = function(e) {
    var file = e.target.files[0];
    if (!file) return;
    var reader = new FileReader();
    reader.onload = function(ev) {
      try {
        var d = JSON.parse(ev.target.result);
        applyData(d);
        autoSave();
        showToast('✅ Dados carregados com sucesso!');
      } catch(err) {
        showToast('❌ Arquivo inválido.');
      }
    };
    reader.readAsText(file);
  };
  input.click();
}

function showToast(msg) {
  var t = document.getElementById('toast');
  if (!t) return;
  t.textContent = msg;
  t.style.opacity = '1';
  t.style.transform = 'translateY(0)';
  setTimeout(function() {
    t.style.opacity = '0';
    t.style.transform = 'translateY(10px)';
  }, 3000);
}

// Intercept all inputs to auto-save
document.addEventListener('input',  function() { autoSave(); });
document.addEventListener('change', function() { autoSave(); });

updateBar();
autoLoad();
setTimeout(updateDash, 200);
</script>

<!-- Toast notification -->
<div id="toast" style="position:fixed;bottom:24px;right:24px;background:#1e1e24;border:1px solid rgba(124,106,247,.4);color:#a89cf8;padding:10px 18px;border-radius:10px;font-size:13px;font-family:'DM Sans',sans-serif;opacity:0;transform:translateY(10px);transition:all .3s;z-index:999;pointer-events:none;box-shadow:0 4px 24px rgba(0,0,0,.4)"></div>
</body>
</html>
</body>
</html>
